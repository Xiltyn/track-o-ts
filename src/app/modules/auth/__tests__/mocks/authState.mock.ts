import { AuthModel } from "../../../../models";

export const authStateMock:AuthModel = {
    user: {},
    token: '',
    statusMessage: '',
    started: false,
    completed: false,
    failed: false,
};
